import { Dimensions } from "features/game/types/craftables";

export const DEFAULT_BUMPKIN_POSITION = {
  x: 2,
  y: -1,
};

export const BUMPKIN_DIMENSIONS: Record<"Bumpkin", Dimensions> = {
  Bumpkin: { width: 1, height: 1 },
};
